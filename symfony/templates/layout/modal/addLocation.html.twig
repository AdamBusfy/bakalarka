{{ form_start(createLocationForm) }}

<div class="modal fade" id="addLocationModal" tabindex="-1" role="dialog" aria-labelledby="addLocationModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="addLocationModalLabel">Create new location</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <div class="form-group">
                    {{ form_row(createLocationForm.name, {
                        attr: {
                            class: "form-control"
                        }
                    }) }}
                </div>
                <div class="form-group">
                    {{ form_row(createLocationForm.parent, {
                        attr: {
                            class: "form-control selectpicker w-100",
                            "data-live-search": "true"
                        }
                    }) }}
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                {{ form_row(createLocationForm._token) }}
                {{ form_row(createLocationForm.submitButton) }}
            </div>
        </div>
    </div>
</div>

{{ form_end(createLocationForm) }}

