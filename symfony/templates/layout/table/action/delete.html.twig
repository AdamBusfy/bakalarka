{{ form_start(form, { attr: { class: 'd-inline-block', onsubmit: confirm is defined and confirm ? 'return confirm("Are you sure?");' : 'return true' } }) }}
{{ form_row(form.id, { attr: { value: id } }) }}
{{ form_row(form.submitButton) }}
{{ form_end(form) }}