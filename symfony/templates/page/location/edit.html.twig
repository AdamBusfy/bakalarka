{% extends 'base.html.twig' %}

{% block title %}Edit location{% endblock %}

{% block stylesheets %}
    {{ encore_entry_link_tags('page-editLocation') }}
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('page-editLocation') }}
{% endblock %}

{% block body %}
    <div class="container" style="width: 75%">
        <div class="row">
            <div class="col">
                <div class="card bg-dark">
                    <div class="card-header text-white">
                        Edit Location
                    </div>

                    <div class="card-body bg-white">

                    {{ form_start(editLocationForm) }}

                    <ul class="list-group list-group-flush">

                        <li class="list-group-item">
                            <div class="form-group">
                                {{ form_row(editLocationForm.name, {
                                    attr: {
                                        class: "form-control"
                                    }
                                }) }}
                            </div>
                            <div class="form-group">
                                {{ form_row(editLocationForm.parent, {
                                    attr: {
                                        class: "form-control selectpicker w-100",
                                        "data-live-search": "true",
                                        "data-api-url": selectApiUrlLocations
                                    }
                                }) }}
                            </div>
                            {{ form_row(editLocationForm.submitButton) }}

                        </li>
                    </ul>
                    {{ form_end(editLocationForm) }}
                    </div>

                </div>
            </div>
        </div>
    </div>
{% endblock %}
